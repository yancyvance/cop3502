#ifdef T1
const char *merge(void) {
    // Separate numbers with a single whitespace only.
    // Do not include trailing spaces or newline characters.
    // An example is provided for reference.

    // Merge Sort: What is the state of the array
    // after the sixth merge operation?
    return "1 2 3 4 5";
}
#endif


#ifdef T2
const char *partition(void) {
    // Separate numbers with a single whitespace only.
    // Do not include trailing spaces or newline characters.
    // An example is provided for reference.

    // Partition: What is the state of the array
    // after completing the partition step? In other words,
    // what does the array look like in the final row?
    return "1 2 3 4 5";
}
#endif


#ifdef T3
int version(void) {
    // Binary Exponentiation: Which version is more efficient?
    // Simply change the return number 0 to either 1 or 2.
    return 0;
}
#endif
